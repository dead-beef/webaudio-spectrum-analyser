import { InputFileUrlDirective } from './input-file-url.directive';

describe('InputFileUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFileUrlDirective();
    expect(directive).toBeTruthy();
  });
});
