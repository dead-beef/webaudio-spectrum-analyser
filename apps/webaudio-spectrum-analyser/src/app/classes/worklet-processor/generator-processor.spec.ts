import { GeneratorProcessor } from './generator-processor';

describe('GeneratorProcessor', () => {
  it('should create an instance', () => {
    expect(new GeneratorProcessor()).toBeTruthy();
  });
});
